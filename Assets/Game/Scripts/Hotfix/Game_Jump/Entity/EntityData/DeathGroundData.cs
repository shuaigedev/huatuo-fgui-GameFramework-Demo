using UnityEngine;

namespace Game.Hotfix
{
    public class DeathGroundData : GroundData
    {
        public DeathGroundData(int entityId, int typeId, Vector3 position, int type) : base(entityId, typeId, position, type)
        {
            
        }
    }
}